POSTGRES_PASSWORD=
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=audio_pipeline
POSTGRES_USER=pipeline

# Runtime data root — db, inbox, archive, failed, logs all live here
# Default: ~/data/audio-pipeline
# PIPELINE_DATA_DIR=~/data/audio-pipeline

# Mac Mini services — use full URL (handles SSH tunnels, direct connections, etc.)
# Direct:      WHISPER_SERVICE_URL=http://yoyodyne:8765
# SSH tunnel:  WHISPER_SERVICE_URL=http://localhost:2222
WHISPER_SERVICE_URL=http://yoyodyne:8765

# Direct:      OLLAMA_BASE_URL=http://yoyodyne:11434
# SSH tunnel:  OLLAMA_BASE_URL=http://localhost:4000
OLLAMA_BASE_URL=http://yoyodyne:11434

WEBUI_SECRET_KEY=
